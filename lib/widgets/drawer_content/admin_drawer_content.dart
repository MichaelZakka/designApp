import 'package:flutter/material.dart';

class AdminDrawerContent extends StatelessWidget {
  const AdminDrawerContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
